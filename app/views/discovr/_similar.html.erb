<% similars.each do |similar| %>
  <% photo = @flickr.photo_from_cache(similar.similar) || @flickr.photo(similar.similar_id) %>
  <%= render :partial => 'photo_thumb', :locals => { :photo => photo } %>
<% end %>